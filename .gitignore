# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri build output
src-tauri/target
src-tauri/gen/schemas

# Local model + engine downloads, if ever placed in-tree
*.gguf
*.vrm
*.pmx

# Gateway secrets
server/.env
# Trial keys live only on the box (like .env); the example is committed.
server/trials.json

# Built documentation site
docs-site/dist

# Built web app
web/dist

# Community library data (lives only on the box, like .env)
server/library.json

# Cloud-sync user data (lives only on the box, like .env)
server/users.json
server/sync/
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
