# E44 · Settings, panel by panel

*Estimated voiceover runtime: 1m 17s. Generated from the deck — edit the deck, then re-run build-subtitles.mjs.*

## Slide 1 · ~7s

**Narration:** One reference episode covering every settings panel, so every later video can just say “see E44” instead of re-explaining. Worth bookmarking.

## Slide 2 · ~10s

**Narration:** Eight panels total: General, Providers, Media models, Voice, Memory, Devices, Usage, and Data & updates. We have touched most of these already — this is the map that ties them together.

**On screen / action:** SWITCH TO APP: Settings. Click through all eight panel tabs left to right.

## Slide 3 · ~9s

**Narration:** The search box at the top matches on what a setting does, not what it is called. Type “interrupt” and it finds barge-in, because that is the behavior, even though the word never appears in its label.

**On screen / action:** APP: type “interrupt” in the settings search box, show it lands on barge-in.

## Slide 4 · ~10s

**Narration:** Every panel uses the same draft-and-save model. Changes show an unsaved marker until you press Ctrl+S. Navigate away with unsaved changes and it confirms before discarding them — nothing is written until you tell it to.

## Slide 5 · ~12s

**Narration:** If you only change a handful of settings, make it these: spend caps, the voice engine, speech rewrite, auto-compact, the embedding model, background mode, and how much of the context window memory is allowed to use. Every one of them has come up already in this series for a reason.

## Slide 6 · ~12s

**Narration:** A few things people go looking for and will not find: a per-tool default model, cloud sync, and a memory share above the twenty-five percent ceiling. Each is a deliberate absence, not a missing feature — simplicity, no account to sync to, and protecting the context window on purpose.

## Slide 7 · ~11s

**Narration:** One update worth flagging: new chats now enable the built-in tools by default, rather than starting empty. That is a starting point, not a standing “enable everything forever” switch — you still trim it down per chat, the same habit from Module 7 about fewer tools choosing better.

## Slide 8 · ~6s

**Narration:** Settings covered the app's own configuration. Next we go deep on one panel specifically — Memory — the three scopes it tracks and the budget that keeps it in check.

