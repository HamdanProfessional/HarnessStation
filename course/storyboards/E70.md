# E70 · Writing a new browser shim from scratch

*Estimated voiceover runtime: 1m 26s. Generated from the deck — edit the deck, then re-run build-subtitles.mjs.*

## Slide 1 · ~7s

**Narration:** Hands-on episode. We add one small new command — get clipboard text — twice: once as a real Tauri command in Rust, once as a browser shim. Same invoke call site, unmodified, on both builds.

## Slide 2 · ~9s

**Narration:** The target: get_clipboard_text. A tiny, self-contained command — no dependencies on anything else in the app — so the whole shim pattern from E68 is visible without noise.

## Slide 3 · ~14s

**Narration:** Rust side first. A #[tauri::command] function, using the arboard crate or Tauri's own clipboard plugin to read the system clipboard and return a String or an error. Then the one easy step to forget: add it to generate_handler's list in the invoke_handler chain — a command that exists but isn't listed there is invisible to the frontend and fails with a not-found error that looks nothing like a permissions problem.

**On screen / action:** SWITCH TO EDITOR: open src-tauri/src/lib.rs. Add a small #[tauri::command] near the other simple commands, and add it to the generate_handler![] list.

## Slide 4 · ~13s

**Narration:** Browser side: a new tiny file, web/shims/clipboard.ts, importing registerCommand from core.ts and registering exactly the same command name — get_clipboard_text — backed by the standard navigator.clipboard.readText API. That's the entire shim.

**On screen / action:** EDITOR: create/open web/shims/clipboard.ts — import registerCommand from ./core, call navigator.clipboard.readText().

## Slide 5 · ~12s

**Narration:** One wiring step the new file needs: something has to import it so its registerCommand call actually runs. The other shims are pulled in from a central entry point purely for that side effect — add clipboard.ts alongside secret.ts, opener.ts and the rest there, or it silently never registers.

**On screen / action:** EDITOR: open the shim's entry point (where fs.ts, http.ts etc. are imported for their side effects) and add an import for ./clipboard.

## Slide 6 · ~10s

**Narration:** And the call site, wherever it lives in src/ — a tool, a component, doesn't matter — needs exactly one line, and it's the same line either build reads it in.

## Slide 7 · ~14s

**Narration:** Prove it both ways. npm run tauri dev, trigger the call, confirm it returns whatever's really on your clipboard. Then, separately, npm run web:dev — no Tauri process behind it at all — trigger the exact same call, and confirm the browser's own clipboard API answers it. That side-by-side proof is the whole point of the architecture from E68: the call site never knew which one it was talking to.

**On screen / action:** TERMINAL: run npm run tauri dev — trigger the call, confirm real clipboard text comes back. Then npm run web:dev — same call, confirm it also works with no Rust process running.

## Slide 8 · ~7s

**Narration:** You've now touched both sides of the seam. Module 19 turns to process: how the app is actually tested, and how a real change gets contributed and verified before it ships.

