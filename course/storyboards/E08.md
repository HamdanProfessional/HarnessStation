# E08 · Sending your first message

*Estimated voiceover runtime: 0m 41s. Generated from the deck — edit the deck, then re-run build-subtitles.mjs.*

## Slide 1 · ~5s

**Narration:** You've got a model connected. Let's close the loop and actually talk to it — and cover the three errors you might hit on the way.

## Slide 2 · ~9s

**Narration:** Pick a model from the selector at the top of the chat, type a message, and press Enter. Nothing more to it — this is the plain chat you already know from other apps.

**On screen / action:** SWITCH TO APP: pick a model in the selector at the top of the chat, type a question, press Enter.

## Slide 3 · ~8s

**Narration:** The reply streams in live and renders as formatted markdown — headings, lists, and syntax-highlighted code blocks. Ask it for a snippet to show that off on camera.

**On screen / action:** APP: let a reply stream in that includes a code block, so the markdown + syntax highlighting is visible.

## Slide 4 · ~13s

**Narration:** Three errors catch almost everyone at least once. “No provider configured” means you skipped connecting a model — go back to Providers. “Connection refused” means your local server, Ollama or LM Studio, isn't actually running. And a 401 or 403 means a bad key, or a cloud account with no credit. Each one tells you exactly which of those it is.

## Slide 5 · ~6s

**Narration:** That's a plain chat. Next episode is the big one — we give the model a tool, and watch it stop being a chat box and start being an agent.

